# bench/

Standalone benchmarks comparing SwarmRT to other runtimes.

- `erlang_bench.erl` — Erlang reference for the spawn/send microbench.
  Compare against `examples/`-style sw programs or the C-runtime
  benchmarks in `src/benchmark_*.c`.
